<div class="container d-flex justify-content-center mb-5">

  <div class="container">
    <div class="container text-center mb-5">
      <h2>Aqui estão seus pedidos, <%= current_client.full_name %>:</h2>
    </div>
    <% @orders.each do |order| %>
      <div class="container rounded shadow p-3 w-50 my-3">
        <%= link_to order_path(order.id), class: "btn btn-link text-dark" do %>
          <h2>Pedido: <%= order.code %></h2>
        <% end %>
        <p class="ms-3">Data do evento: <%= l(order.booking_date) %></p>
      </div>
    <% end %>
  </div>
</div>

